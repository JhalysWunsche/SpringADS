server:
  servlet:
    context-path: /
  port: 8080
spring:
  application:
    name: SpringADS
  datasource:
    url: jdbc:h2:mem:SpringADS
    driverClassName: org.h2.Driver
    username: sa
    password:


  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
